# 功能清单

最后核对：2026-09-20。

按用户可独立使用的能力划分，当前共 **7 类、29 个功能点**。按钮、接口、样式和错误提示不重复计数；本文件记录已实现行为，不是待办计划。安装与具体操作见 [README](README.md)。

## 1. 数据准备（2 项）

1. **本地 PBF 建索引**：读取一个本地 PBF，提取铁路、地铁、轻轨、单轨铁路及相关关系、车站。生成索引后，运行服务不再依赖原 PBF。
2. **链接下载并建索引**：支持多连接、断点续传、重定向，无需 aria2；下载到系统 Downloads 下。一次处理一个数据源，覆盖索引，不合并多个 PBF。

## 2. 地图查找（5 项）

3. **在线地点搜索**：通过 Nominatim 搜索城市、车站、机场和地址，选择后定位地图；不受本地 PBF 覆盖范围限制。
4. **本地搜索历史与键盘选择**：保留最近 10 条搜索历史，可清空。搜索结果、历史列表支持方向键选择和回车确认。
5. **附近轨道查询**：右键选择 100／250／500 米范围。有名称的优先、同名相邻、无名靠后；名称组按组内最近距离排序，组内按距离排序。结果超过接口上限时仅显示最近的一批，再对这批结果分组。
6. **按 way ID 选轨道**：输入已知 OSM 轨道 ID，直接选择轨道。
7. **按可视范围显示车站**：地图放大到 16 级后显示车站图标，悬浮查看站名和线路；不绘制建筑或站台轮廓。

## 3. 线路查看（4 项）

8. **直接所属关系**：从附近轨道或当前轨道查看所属关系、已有的起终点和环线信息；有起终点信息的关系优先。点击关系只更新地图预览和选中状态，保留轨道详情。
9. **关系内相连轨道筛选**：以所选 way 为依据，只预览同一关系内与它相连的轨道，显示隐藏数量，也可查看完整关系。相连不保证运行方向相同。
10. **轨道与关系预览、定位**：高亮对应几何，定位按钮缩放到对应范围，候选轨道支持悬浮预览。
11. **OSM 方向箭头**：使用 `oneway` 或 `railway:preferred_direction` 展示方向依据；无明确标签时不凭节点顺序猜运行方向。首选方向不等于单行限制。

## 4. 行程编辑（8 项）

12. **开始行程**：每段首条 way 只加入自身，不自动推进，留出确认位置、起点和方向的机会。
13. **分支选择与自动推进**：点击候选轨道名称后加入轨道，并沿唯一明确的前进连接继续。遇分支、闭环、折返或方向不明时停止；星标表示推荐，不限制手动选择。
14. **单条前进**：候选右侧双右箭头只加入这一条 way，不连续推进。
15. **退回一条轨道**：移除当前段末尾一条 way，不移动地图。末尾已截断时移除保留部分；不是通用撤销，不恢复整次截断。
16. **独立换乘分段**：“作为下一段继续”保留已有行程，从不相连的轨道开启新段；地图和 KML 不强行连接换乘处。
17. **指定中途起点及方向**：当前段仅有首条轨道时，右键“从此开始本段”，将位置投影到轨道，再从 A／B 两部分选择方向。不指定起点时仍可按整条轨道开始。
18. **中途结束本段**：右键“在此结束本段”，投影到本段已选轨迹，移除该点之后的部分和后续整条 way。需在轨迹 150 米内且已确定方向；截取不移动地图。
19. **清空全部行程**：清除所有行程段，不移动地图。行程仅在页面内存中，刷新也会清空，不自动恢复。

## 5. 批量连接（3 项）

20. **沿关系连续加入**：从当前轨道查看并选择关系，沿已确定方向加入无歧义的连续部分，到关系缺口、歧义连接或已选轨迹前停止。首条完整 way 位于连续链端头时可确定朝链内的方向。
21. **跨线寻路**：附近轨道的链条按钮寻找从当前段到目标轨道的顺向连接；失败时最多尝试退回 10 条 way，优先少回退，再比较实际距离。预览新增、移除部分，确认后接入当前段。
22. **同线替代汇入点推荐**：精确连接失败时，沿目标同线连续部分寻找可达位置，展示原位置、推荐点及偏移方向和距离；需明确确认改接。遇同线分支或歧义连接停止扩展。

跨线寻路使用本地 PBF 共享节点，每次尝试最多 50 公里，另有搜索量上限。不核实列车班次、实际站台股道或调度可行性；搜索失败不代表现实铁路不通。

## 6. 导出（5 项）

23. **导出范围选择**：选择全部行程或其中一段。
24. **KML 导出**：一个文件保留多段独立轨迹、实际截断位置和颜色，换乘处不连线。
25. **本地车站建议与编辑**：为起点、换乘点和终点提供最多 5 个候选，可手动填写。优先使用本地 `name`，按轨道停车点或运行线路停靠站关联；缺关联时使用附近车站建议，不在线补站名。
26. **换乘站名与文件名整理**：生成 `A → B → C.kml`；换乘两站不同名时使用 `A → B1（B2） → C`，相同名称不重复，可删除多余站名项。同一 stop_area 的同名候选可合并，不使用 stop_area_group 合并不同站名。
27. **Overpass 查询查看与复制**：在“更多”中查看、复制查询。Overpass 返回完整 way，无法表达截断范围；复制含截断轨道的查询时提醒使用 KML 保留准确位置。

## 7. 行程展示（2 项）

28. **行程统计**：展示各段线路名、轨道数量、距离和总距离，随前进、退回、截取更新。“行程段”与“轨道条数”分别计数。
29. **线路色与分段配色**：优先使用起始轨道的有效线路色，缺失时按行程段轮换颜色，地图与 KML 一致。

## 支撑能力与边界

以下不单独计入 29 项功能：

- 普通文本日志、请求与页面操作关联、日志轮转。
- 数据源版本校验，避免旧页面混用新索引。
- 轨道按需加载及页面内缓存、过期响应拦截、取消连接查找。
- 普通行程计算由前端完成；跨线寻路由后端一次请求计算并返回方案，前端确认应用。后端不保存行程会话或 Cookie。
- 即时悬浮提示、禁用原因、操作反馈，以及 Python／JavaScript 自动化测试。
- 本地轨道、关系和车站仅覆盖当前 PBF；在线底图、地名搜索和外部前端资源需要网络。
- 页面不展示原始标签；接口只保留前端名称、类型、颜色、方向及推荐所需的标签，后端索引保留完整数据。

## 维护约定

新增、删除功能或修改行为边界时，同步更新本清单；安装命令与操作步骤维护在 README。修复样式、替换按钮或调整实现位置不自动增加功能数量。
